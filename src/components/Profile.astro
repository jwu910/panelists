---
import { Image } from "@astrojs/image/components";

interface Props {
  height?: number;
  src: ImageMetadata;
  name: string;
  width?: number;
  otherProps?: any;
}

const { src, name, width, height, ...otherProps } = Astro.props;
---

<Image
  alt={`${name} Photo`}
  class="image"
  height={height || 128}
  src={src}
  style="border-radius: 50%;"
  width={width || 128}
  {...otherProps}
/>
